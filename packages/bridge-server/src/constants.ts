import * as path from "path";

export const APP_NAME = "PI_FRAME_BRIDGE_SERVER";
export const PORT = 9001;
export const ENTRY_DIR = __dirname;
export const PUBLIC_DIR = path.resolve(ENTRY_DIR, "public");
